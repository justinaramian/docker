# Your base domain, e.g. example.com
DOMAIN=

# Encryption key for stored integration credentials.
# Generate: openssl rand -hex 32
HOMARR_SECRET_ENCRYPTION_KEY=

# Authentik OIDC provider
# Authentik admin > Applications > Providers > Homarr
HOMARR_OIDC_CLIENT_ID=
HOMARR_OIDC_CLIENT_SECRET=